/**
 * Tailwind CSS Configuration (v4)
 *
 * This config file now only contains Plugins that extend Tailwind functionality
 */

export default {
  plugins: [require('@tailwindcss/forms'), require('@tailwindcss/typography')],
};
